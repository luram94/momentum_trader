# Core dependencies
# (touching this file forces Streamlit Cloud to rebuild the environment and
# restart the app process, which a plain code push does not)
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Data sources
finvizfinance>=0.14.0
yfinance>=0.2.0

# Web framework (Streamlit)
streamlit>=1.28.0
plotly>=5.18.0

# Configuration
pyyaml>=6.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
