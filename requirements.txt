# Runtime dependencies (dev/test tools live in requirements-dev.txt)
# (touching this file forces Streamlit Cloud to reprocess dependencies and
# restart the app process, which a plain code push does not do when the
# previous process has crashed or holds stale module imports)
# Last forced restart: 2026-07-13 (stale hqm.config_loader after PR #11)
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Data sources
finvizfinance>=0.14.0
yfinance>=0.2.0

# Web framework (Streamlit)
streamlit>=1.28.0
plotly>=5.18.0

# Configuration
pyyaml>=6.0
