services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      # Persist the SQLite database and logs across container rebuilds
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
